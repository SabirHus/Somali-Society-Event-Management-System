{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:8080", // Target URL for launching the browser
            "webRoot": "${workspaceFolder}" // Root directory for source mapping
        }
    ]
}